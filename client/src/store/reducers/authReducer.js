const initState = {
    isLoggedIn: true,
    token: null
}

const authReducer = (state = initState, action) => {
    switch (action.type) {

        default:
            return state;
    }
}

export default authReducer